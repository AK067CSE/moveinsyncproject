package com.moveinsync.billing.model.enums;

public enum BillingModelType {
    PACKAGE,    // Fixed monthly cost
    TRIP,       // Per-trip or distance-based
    HYBRID      // Combination of above
}
